@model WebsiteBanHang.Models.Category

@{
    ViewData["Title"] = "Cập nhật danh mục";
}

<div class="row justify-content-center">
    <div class="col-md-6">
        <div class="card border-0 shadow-sm" style="border-radius: 12px;">
            <div class="card-header bg-white pt-4 px-4 border-0">
                <h5 class="fw-bold mb-0"><i class="fas fa-edit text-warning me-2"></i>Cập nhật danh mục</h5>
            </div>
            <div class="card-body p-4">
                <form asp-action="Update" method="post">
                    <input type="hidden" asp-for="Id" />
                    <div class="mb-3">
                        <label asp-for="Name" class="form-label fw-semibold small text-muted text-uppercase">Tên danh mục</label>
                        <input asp-for="Name" class="form-control" style="border-radius: 8px;" />
                        <span asp-validation-for="Name" class="text-danger small"></span>
                    </div>
                    <div class="mb-4">
                        <label asp-for="FolderName" class="form-label fw-semibold small text-muted text-uppercase">Thư mục ảnh</label>
                        <input asp-for="FolderName" class="form-control" style="border-radius: 8px;" />
                    </div>
                    <div class="d-flex justify-content-between">
                        <a asp-action="Index" class="btn btn-light px-4" style="border-radius: 8px;">
                            <i class="fas fa-arrow-left me-2"></i>Quay lại
                        </a>
                        <button type="submit" class="btn btn-warning px-4" style="border-radius: 8px;">
                            <i class="fas fa-save me-2"></i>Cập nhật
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
